<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../../public/styles/css-reset.css" />
    <link rel="stylesheet" href="../../public/styles/category-page.css" />
    <title><%= pageTitle %></title>
  </head>
  <body>
    <%- include("../layout/navbar")%>
    <h1>Create category</h1>
    <form
      id="add-book-category-form"
      action="/api/category/new"
      method="post"
      enctype="multipart/form-data"
    >
      <div class="name-field">
        <label for="name">Name *:</label>
        <input
          type="text"
          name="categoryName"
          id="name"
          placeholder="Fantasy, Mystery, Thriller"
          required
        />
        <p class="category-error">
          <% if(locals.errors.length > 0){ %> <%locals.errors.forEach(item =>
          {%>
          <span>
            <%if(item.path==='categoryName'){%> <%= item.msg %> <%}%>
          </span>
          <%}); %> <%} %>
        </p>
      </div>
      <div class="description-field">
        <label for="description">Description *:</label>
        <textarea
          name="categoryDescription"
          id="description"
          cols="30"
          rows="10"
          required
        ></textarea>
      </div>
      <input
        type="file"
        name="categoryimg"
        accept="image/*"
        id="categoryimg"
        required
      />
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
